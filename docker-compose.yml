services:
  postgres:
    image: postgres:16
    container_name: chatwar-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: chatwar
    ports:
      - "5432:5432"
    volumes:
      - chatwar_postgres:/var/lib/postgresql/data

volumes:
  chatwar_postgres:
